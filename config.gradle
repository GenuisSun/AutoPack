// 把敏感信息存放到自定义的properties文件中
def propertiesFile = rootProject.file("config.properties")
def properties = new Properties()
properties.load(new FileInputStream(propertiesFile))
ext {
    // 调试签名信息
    KEY_MOBILE_ALIAS = properties['KEY_MOBILE_ALIAS']
    KEY_FILE = properties['KEY_FILE']
    KEY_PASSWORD = properties['KEY_PASSWORD']

    //版本号
    VERSION_CODE = properties['VERSION_CODE']
    VERSION_NAME = properties['VERSION_NAME']
    //360账号密码
    ACCOUNT_360 = properties['ACCOUNT_360']
    PASSWORD_360 = properties['PASSWORD_360']

    //360加固保 首次配置自动下载  下方链接为官方链接，如官方有改动  可改
    URL_JIAGU_ZIP_360 = [mac    : properties['URL_JIAGU_ZIP_360_MAC'],
                         windows: properties['URL_JIAGU_ZIP_360_WIN'],
                         linux  : properties['URL_JIAGU_ZIP_360_LINUX'],
    ]
    //蒲公英配置
    pgy = [apiKey   : properties['PGY_APIKEY'],
           uploadUrl: properties['PGY_UPLOADURL']]
}